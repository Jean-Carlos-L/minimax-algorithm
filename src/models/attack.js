class Attack {
  constructor(name, type, power) {
    this.name = name;    // nombre de la habilidad 
    this.type = type;     // Tipo del ataque 
    this.power = power;   // Poder base del ataque 
  }
}

module.exports = Attack;